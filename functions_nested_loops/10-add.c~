#include "main.h"
/**
 * int add - function that adds two integers and returns the result
 * @a: number input
 * @b: number input
 * Return: add = a + b
 */
int add(int a, int b)
{
int add;
add = a + b;
return (add);
}
