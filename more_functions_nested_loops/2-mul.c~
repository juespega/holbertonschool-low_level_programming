#include "main.h"
/**
 * mul - a function that multiplies two integers.
 * @a: input number
 * @b: input number
 * Returns: result of a * b
 */
int mul(int a, int b)
{
int mul;
mul = a*b;
return (mul);
}
