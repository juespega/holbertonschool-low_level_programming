#include "main.h"
/**
 * Write a function that swaps the values of two integers.
 *
 * @a: input pointer
 * @b: input pointer
 * Return: value swapped
 */
void swap_int(int *a, int *b)
{
int aux = 0;
aux = *a;
*a = *b;
*b = aux;
}
